export type Testimonial = {
  quote: string;
  author: string;
  role?: string;
};

/**
 * Depoimentos reais somente. Não inventar avaliações.
 * Quando o cliente autorizar a publicação, incluir os objetos neste array.
 */
export const testimonials: Testimonial[] = [
  // [INSERIR DEPOIMENTO]
];
